<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content">

    <!--<com.vrv.linkdood.ui.view.chat.BubbleImageView
        xmlns:app="http://schemas.android.com/apk/res-auto"
        android:id="@+id/img_chat_image"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:scaleType="centerCrop"
        app:bubble_angle="4dp"
        app:bubble_arrowHeight="8dp"
        app:bubble_arrowLocation="right"
        app:bubble_arrowOffset="1dp"
        app:bubble_arrowTop="16dp"
        app:bubble_arrowWidth="8dp" />-->

    <ImageView
        android:id="@+id/img_chat_image"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:src="@mipmap/vim_pictures_default"
        android:scaleType="fitCenter"
        android:adjustViewBounds="true"
        android:maxHeight="120dp"
        android:maxWidth="120dp"
        />

    <!-- 显示 发送文件，图片的进度 -->
    <ProgressBar
        android:id="@+id/progress_chat_loading"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:visibility="gone" />
</FrameLayout>